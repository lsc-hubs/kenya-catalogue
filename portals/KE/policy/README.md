Wageningen, 28-2-2024

Information has been extracted by LSC-hubs project WP2 and enriched via web search

Export index.csv to mcf using

```
pip install geodatacrawler
crawl-metadata --mode=import-csv --dir=. --sep=';'
```